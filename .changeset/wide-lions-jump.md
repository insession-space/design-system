---
"@insession/design-system": minor
---

レイアウトプリミティブ（`Stack` / `VStack` / `HStack` / `Grid` / `Spacer` / `Divider` / `Center` / `Container`）を追加した。`gap`（Stack / Grid）と `columns`（Grid）はブレークポイント別に指定できるレスポンシブ値（`Responsive<T>`）を受け付ける。
