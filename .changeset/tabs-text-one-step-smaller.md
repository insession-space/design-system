---
'@insession/design-system': minor
---

feat(tabs): タブのラベルを一段小さくする

`Tabs.Tab` のラベルを `text-base`(14px) から `text-sm`(12px) へ一段下げ、あわせて上下パディングを `py-3`(12px) から `py-2.5`(10px) に詰めた。下線タブの主張を弱め、タブ行の密度を落ち着かせる。

見た目の既定が変わるため、消費側でタブ行の高さが約 7px 縮む。`size` prop は追加していないので、従来の大きさに戻す手段は無い（必要なら別途 prop 化を検討する）。
