{
  "name": "@in-session/ui",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "types": "dist/index.d.ts",
  "description": "InSession の純粋 leaf UI（アプリ依存なし。i18n 等は props で注入する）",
  "//": "ソースのまま Vite が解決する（事前ビルドしない）。react はホスト（apps/web）と同一インスタンスを使うため peerDependency。",
  "exports": {
    ".": "./index.ts",
    "./theme.css": "./theme.css"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "typescript": "^5.7.0"
  }
}
